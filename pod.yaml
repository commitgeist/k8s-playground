apiVersion: v1
kind: Pod
metadata:
  name: podrhama
spec:
  containers:
  - name: podrhama
    image: rhamanalysis/rhama-hidro-authenticator-app:production-10096
    resources:
      limits:
        memory: "300Mi"
        cpu: "1.0"
      requests:
        memory: "150Mi"
        cpu: "0.5"
  imagePullSecrets:
  - name: docker-hub-secret
